# Ses Efekti & İstatistik Paneli Tasks

- [ ] Mobile: `npx expo install expo-av` ile ses paketi kurulacak.
- [ ] Backend: `index.js` içerisine `GET /api/signals/stats` endpointi yazılacak ($30'lık kasa büyümesi, TP/SL sayısı hesaplanacak).
- [ ] Mobile: `src/screens/StatisticsScreen.js` oluşturulacak. Tasarımında Toplam Kazanç, Başarı Oranı vb. görsel grafikler/kartlar olacak.
- [ ] Mobile: `AppNavigator.js` güncellenerek alt menüye 3. sekme olarak "İstatistik" eklenecek.
- [ ] Mobile: `HomeScreen.js` güncellenerek önceki fetch state'i hafızaya alınacak. Sinyal statüsü `WIN` olursa Trink sesi, `LOSS` olursa Dııt sesi çalınacak.
- [ ] Backend yeniden başlatılacak ve sistem test edilecek.
