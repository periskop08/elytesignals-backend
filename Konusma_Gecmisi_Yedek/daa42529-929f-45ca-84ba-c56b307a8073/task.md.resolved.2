# Elyte Mobile Parity Redesign Tasks

- [x] Create `mobile/src/components/TopHeader.js` (Web-like dark header with Logo, ELYTE text, Chat Icon, Avatar).
- [x] Create `mobile/src/components/SignalCard.js` (Web-like glassmorphic card with top icon, flex header, and bottom 3-column dark box).
- [x] Refactor `mobile/src/navigation/AppNavigator.js` to use only 3 Bottom Tabs (Markets, Favorites, Stats) and web-like tab styling.
- [x] Convert `AnalysisScreen` into a global Modal accessible via the `TopHeader` chat icon.
- [ ] Update `HomeScreen.js` to use `TopHeader` and `SignalCard`.
- [ ] Update `FavoritesScreen.js` to use `TopHeader`, `SignalCard`, and web-like Personal Performance 2x2 grid.
- [ ] Update `StatisticsScreen.js` to strictly match the web stats layout.
- [ ] Delete `DashboardScreen.js` as it is redundant.
