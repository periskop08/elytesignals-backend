# Görsel Kusursuzluk ve Canlı Fiyat Mimarisi Task

- `[ ]` 1. `Dashboard.jsx` Akıllı Küsurat (Decimal) Çözümü
    - `[ ]` `fmtPrice` fonksiyonunu modifiye ederek gelen fiyatın büyüklüğüne göre dinamik ondalık atamasını ayarla (Örn: `1000'den büyükse 2, 10'dan büyükse 3, altındaysa 5 hane`).
- `[ ]` 2. TradingView Widget Varlık Tercümanı
    - `[ ]` Sinyal kartı içerisindeki grafik (iframe) URL'sini temiz sembolden TradingView Ticker'larına (Örn: `NASDAQ:NDX`, `SP:SPX`, `OANDA:XAUUSD` vb.) dönüştürecek yeni bir `getTradingViewSymbol()` fonksiyonu yaz.
- `[ ]` 3. `backend/index.js` Canlı Fiyat Proxy (Veri Yok Hatası)
    - `[ ]` `/api/prices/assets` bloğunda bulunan statik 6'lı `ASSET_BINGX_MAP`'i sil.
    - `[ ]` Taranan tüm güncel listeyi (48 asset) kapsayabilmesi için `global.BINGX_SYMBOL_MAP` ile ters eşleştirme yaparak Canlı fiyatları API'ye döndür.
- `[ ]` 4. AWS Dağıtım & Doğrulama
    - `[ ]` Backend'i `deploy.sh` ile, Frontend'i de Vercel/SSH ile canlıya al.
    - `[ ]` `walkthrough.md` raporunu yaz.
