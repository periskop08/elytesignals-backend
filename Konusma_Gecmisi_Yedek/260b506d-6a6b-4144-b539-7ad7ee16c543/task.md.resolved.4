# Google Sheets - Yeni Şema Sistem Görevleri

- [x] `backend/google-api.js` dosyasının `APPEND` metodu satır bazlı (Bulk Insert veya çoklu sıralama) veriye göre güncellenecek.
- [x] `backend/scanner.js` içindeki `sendNightlyReport` algoritması parçalanıp yeni 7 sütunluk formata (Skor Puanı, Sinyal Sayısı, TP, SL, WR, Toplam Sinyal Sayısı, Tarih) uygun çift boyutlu Array üretecek şekilde yazılacak.
- [x] Oluşturulan kodlar başarıyla çalışıyorsa `~/.ssh/Elyte.pem` SSH anahtarı ile Amazon (`13.60.44.209`) sunucusuna transfer edilip (`Rsync`) canlıya alınacak.
- [x] Sunucudaki `PM2` servisi restart edilerek yeni Google Sheets bağlantısı devreye sokulacak.
