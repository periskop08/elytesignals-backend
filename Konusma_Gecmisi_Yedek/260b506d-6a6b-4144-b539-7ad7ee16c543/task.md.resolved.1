# Canlı Amazon Sunucusu Dağıtım Görev Listesi

> [!WARNING]
> Amazon EC2 sunucu IP adresi ve SSH bağlantı anahtarı ('.pem' formatlı dosya) bekleniyor.

- [ ] Canlı Amazon (AWS) sunucusuna SSH tüneli bağlantısı testi yapılacak.
- [ ] Yerel bilgisayardaki güncel `scanner.js`, `google-api.js`, `.env` ve `package.json` dosyaları `Rsync/SCP` kullanılarak üretim sunucusuna transfer edilecek.
- [ ] Üretim sunucusundaki SQLite `signals.db` veritabanına SQL komutu atılarak "35" skorlu yanlış/düşük sinyaller kalıcı olarak silinecek.
- [ ] Yeni sistemin (40 Puan Limiti + Google Sheets) aktifleşmesi için sunucudaki arka plan servisi (örneğin PM2) yeniden başlatılacak.
