- `[ ]` 1. Implement `analyzeOptionsFlow()` asynchronous function using `yahooFinance.options`.
- `[ ]` 2. Add sub-logic to find nearest expiration date and extract Put & Call Data.
- `[ ]` 3. Implement Put/Call Ratio (PCR) calculation.
- `[ ]` 4. Implement Max Pain Calculation logic (minimal intrinsic value).
- `[ ]` 5. Implement Open Interest Peak (Call Wall & Put Wall) calculation.
- `[ ]` 6. Inject `analyzeOptionsFlow()` into `scanner.js` -> `analyzeCoin()`.
- `[ ]` 7. Apply Score adjustments (+8, +7, ±10) only for Equities.
- `[ ]` 8. Write a quick local validation script `test-options.js` to ensure the logic runs smoothly before pushing.
