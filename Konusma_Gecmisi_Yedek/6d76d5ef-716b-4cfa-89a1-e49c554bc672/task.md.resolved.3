# Max Trade Limit & Backfill Queue Implementation Tasks

- [/] Add `MAX_ACTIVE_TRADES` and `PRICE_TOLERANCE_PCT` to config in `backend/scanner.js`
- [ ] Implement `backfillTrades()` in `backend/scanner.js` to process waiting signals ordered by `qualityScore`
- [ ] Add limit checks (max 10) inside `runScan()` before placing new orders
- [ ] Call `backfillTrades()` at the end of `checkActiveSignals()`
- [ ] Export `backfillTrades` and trigger it in `backend/index.js` inside `/api/user-trades/close` endpoint
