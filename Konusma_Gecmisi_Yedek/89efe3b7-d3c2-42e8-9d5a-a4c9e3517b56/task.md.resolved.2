# Weekday/Weekend Hybrid Asset Fetching

- [ ] Update `fetchAssetIntervalData` to catch `BingX` empty arrays.
- [ ] Implement Yahoo Finance `.chart()` call in the catch block to fetch historical intraday asset data when BingX is paused.
- [ ] Update `index.js` on the Remote Server and restart the backend.
- [ ] Test the backend with `curl` for assets (e.g. `AAPL`) and verify Yahoo Finance fallback kicks in and returns an analysis on weekends.
